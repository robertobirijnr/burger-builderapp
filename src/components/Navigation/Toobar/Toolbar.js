import React from "react";
import "./Toolbar.css";

const Toolbar = () => (
  <header className="Toolbar">
    <div>Menu</div>
    <div>LOGO</div>
    <div>...</div>
  </header>
);

export default Toolbar;
