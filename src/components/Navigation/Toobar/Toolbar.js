import React from "react";
import "./Toolbar.css";
import Logo from "../Logo/Logo";

const Toolbar = () => (
  <header className="Toolbar">
    <div>Menu</div>
    <Logo />
    <div>...</div>
  </header>
);

export default Toolbar;
